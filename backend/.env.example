# Example environment variables for backend
# Set to path of your Google Service Account JSON file (do NOT commit real keys)
GEE_SERVICE_ACCOUNT_FILE=C:\path\to\your\service-account.json

# Optional: explicitly enable fallback heuristics (true/false)
USE_FALLBACK=true
